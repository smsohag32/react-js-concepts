// how to declare a variable using let and const 

const num = 7;

// condition < > , ===, !=

// multiple conditions : && ||

if (num === 0 || num > 10) {
    return
}
else {
    return;
}

// array declare
// index
// length, push

// loop ( for loop , for in loop, while loop)


// object declare 
const students = {
    name: 'sohag',
    id: 55
}
// ways to access property by name
// student.age direct by property
// student['age'] access via property name string
// student[variableName]

// function declare (arrow function, regular function)

const some = (num1, num2) => {
    return num1 + num2;
}


