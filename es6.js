// let const

// template string

const about = `My name is sheikh ${okay} ${students.name} also like`

// arrow function declaration .

const sum = (num1, num2) => {
    return num1 + num2;
}
const name = () => {
    return 'sohag'
}
// is even 
const isEven = x => x % 2 == 0;

// spread operator (...number)
// create an new array from an older array and add an element
const currentNumber = [...Number, 59];

