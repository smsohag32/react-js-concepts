// array destructuring

const numbers = [54, 55];

// const [x, y] = [54, 55];
const [x, y] = numbers;

function boxify(num1, num2) {
    const nums = [num1, num2];
    return nums;
}

// objects destructuring

const { name, age } = { name: 'sohag', age: 26 }

// optional chaining using
